# 2026-07-09 工作日志

## TitleBar 重构

### 窗口控制按钮抽离为独立组件
- 将 `index.vue` 中的最小化/最大化/关闭按钮及相关逻辑抽离为 `src/layout/components/TitleBar/WindowControls.vue`
  - 包含三个按钮的 template、`isMaximized` 响应式状态、窗口操作方法（`minimize`/`toggleMaximize`/`closeWindow`）、最大化状态监听
  - 独立 scoped 样式
- `index.vue` 清理：移除被抽离的 template、script 逻辑、未使用的 CSS（`.title-bar__controls`、`.title-bar__btn` 等）

## 单实例锁

- `.env` 新增 `VITE_SINGLE_INSTANCE_LOCK=false` 环境变量（默认关闭）
- `src/electron/main/index.ts` 新增条件单实例锁逻辑：环境变量为 `true` 时调用 `app.requestSingleInstanceLock()`，并监听 `second-instance` 事件激活已有窗口（restore + focus）
- 环境变量设为 `false` 时完全跳过，不干扰开发调试多实例

## 目录结构调整

- 拍平 `src/electron/main/` 目录：`main/index.ts` → `main.ts`，`main/settings.ts` → `settings.ts`
- 删除 `src/electron/main/` 空目录
- 更新 `build/plugins/electron-plugin.ts` 中的 entry 路径指向 `src/electron/main.ts`
- 类型检查通过，未造成编译错误

## 类型目录重构

- 新建 `src/types/electron/` 目录，按模块拆分 Electron preload 类型声明
- 新建 `src/types/electron/control.d.ts` — 内联 `control` 类型，直接扩充 `Window` 接口
- 新建 `src/types/electron/app.d.ts` — 内联 `app` 类型，直接扩充 `Window` 接口
- 新建 `src/types/electron/settings.d.ts` — 引入 `SettingsAPI`，扩充 `Window.setting`
- 删除 `src/types/global/global.d.ts`（原整合文件）
- 统一 Window 属性命名：`windowControls` → `control`、`appInfo` → `app`、`settingsAPI` → `setting`，同步更新 `preload.ts`、类型声明及所有 renderer 引用
- 重命名 `src/electron/settings.ts` → `setting.ts`，保持单数形式一致
- 拆分 `src/electron/preload.ts` 为 `src/electron/preload/` 目录（control.ts / app.ts / setting.ts + index.ts 入口），构建入口改为 `preload/index.ts`，删除原 `preload.ts`
- 新增剪贴板功能：`src/electron/preload/clipboard.ts`（preload 绑定）、`src/types/electron/clipboard.d.ts`（类型声明），handler 直接内联在 `main.ts` 中，暴露 `window.clipboard.readText()` / `window.clipboard.writeText()`

## 主进程 IPC 解耦重构
- 新建 `src/electron/ipc/window.ts` / `settings.ts` / `clipboard.ts`，分别导出 `initWindowIpc(getWindow)` / `initSettingsIpc()` / `initClipboardIpc()`，把原 `main.ts` 中内联的 `ipcMain.on/handle` 监听按模块拆出。
- `main.ts` 仅保留窗口创建、单实例锁、最大化状态广播，并在 `bootstrap()` 内调用三个 `initXxxIpc()` 装配。
- 窗口实例通过惰性 getter `() => mainWindow` 注入 `initWindowIpc`；handler 内统一 `const win = getWindow(); if (!win) return` 守卫，去掉原先大量的 `?.` 判断（遵循用户要求的守卫写法约定）。
- 类型检查：`vue-tsc -b` 仅剩一处与本次无关的预存错误 `src/types/index.ts` 中 `ThemeMode` 导入缺失，本次改动未引入新错误。
- 修正：内联 IPC 回调改回箭头函数（原 `main.ts` 即用 `() =>` 风格），`function` 声明仅用于模块级具名函数（如 `initWindowIpc`），不强行一刀切；同步更新 `MEMORY.md` 函数声明规范。
- 再修正：`window.ts` 的窗口 IPC 处理函数改回 `getWindow()?.method()` 可选链写法，取消 `if (!win) return` 守卫（用户要求）；同步更新 `MEMORY.md` 的 IPC 约定。
