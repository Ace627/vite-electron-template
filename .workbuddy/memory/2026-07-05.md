# 2026-07-05 工作日志

## 更新 README.md

基于完整代码审查，全面重写了 README.md，修正了以下与最新代码不一致的问题：

1. **脚本命令修正**：`pnpm dev` → `pnpm dev:electron`，`pnpm build` → `pnpm build:web`，移除了不存在的 `pnpm preview`
2. **打包输出路径修正**：`dist-electron/release/` → `dist-release/`（与 electron-builder.yaml 的 `output` 配置一致）
3. **补充缺失的目录结构**：添加了 `build/plugins/`、`.env`/`.env.production`、`.npmrc`、`uno.config.ts` 等
4. **新增章节**：环境变量表、插件系统说明、安全架构说明
5. **技术细节对齐**：确认了 Element Plus 版本、Sass 预处理器、Rolldown 压缩、SCSS 全局注入等配置

所有描述均严格对照 `src/`、`build/`、配置文件的代码实际内容编写。

## 再次更新 README.md（二次同步）

应 `/sync-readme` 请求，通过 Explore 子智能体全面审查所有源文件后再次更新：

1. **技术栈补充**：新增 Axios (1.18.1) 和 dayjs (1.11.21) 依赖信息
2. **新增 SVG 图标系统**：补充了 `vite-plugin-svg-icons-ng`、`SvgIcon` 全局组件、`src/assets/svg-icons/` 图标清单
3. **新增 `src/plugins/` 插件注册**和 `src/components/SvgIcon/` 组件说明
4. **补充 `src/utils/validate.ts`** 和 `build/plugins/svg-icons-plugin.ts`
5. **新增 UnoCSS 自定义规则**（wh-/mtb-/mlr-等）和快捷方式
6. **新增 MODE 环境变量**
7. **新增图标使用规范**到项目约定章节
8. **精确到次版本的版本号**（如 ^3.5.39、66.7.4、2.14.2 等）

## 创建 skill：sync-readme

将上述 README 同步更新的完整工作流做成 skill（`sync-readme`），存储在用户级 skill 目录 `~/.workbuddy/skills/sync-readme/`。包含完整的四步执行流程（代码探索 → 逐项比对 → 更新写入 → 验证交付）和 13 个必选章节模板。

## SVG 图标系统适配

补全项目已有的图标基础设施，使 `vite-plugin-svg-icons-ng` 方案完整可用（默认 `htmlMode: 'inline'` 自动注入雪碧图，无需手动导入虚拟模块）：

1. **`src/views/dashboard/index.vue`** — 概览卡片使用 `SvgIcon` 组件，Cpu/Expand/Fold/Plus 各配一个图标
2. **项目记忆** — 追加图标使用规范（SvgIcon 组件 + name 指向 svg-icons 文件名、Element Plus 组件通过插槽接入）
3. 移除之前误加的 `import 'virtual:svg-icons-register'` 和不必要的 tsconfig 类型声明
