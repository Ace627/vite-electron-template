# 2026-07-04 工作记录

## 完成内容

### Electron 集成
- 配置 vite-plugin-electron/simple，main + preload 入口
- 实现自定义 TitleBar（无边框窗口、拖拽、窗口控制）
- 实现原生 about 面板 → 回退到自定义 AboutDialog
- 帮助菜单：关于对话框 + 开发者工具切换
- 窗口控制 IPC 链路（minimize/maximize/close/openDevTools）

### UI 组件
- TitleBar 组件：自定义标题栏（拖拽区、窗口控制按钮、帮助菜单）
- AboutDialog 组件：展示应用版本和环境信息，支持复制

### 类型系统
- 统一 WindowControls / AppInfo 类型声明
- 通过 contextBridge 桥接 preload → 渲染进程

### 模板化改造
- 项目名改为 .env 驱动（VITE_APP_TITLE）
- 移除所有硬编码项目名
- 编写 README.md
- electron-builder.yaml 泛化（appId + productName 跟随 .env）

### 打包
- 安装 electron-builder
- 配置 electron-builder.yaml（NSIS 安装向导）
- 图标尺寸问题待解决（需 256x256 PNG）

### 项目约定
- MEMORY.md 记录：TipModal 封装、自动导入、类型规范、色彩规范

### 设置系统
- 新建 src/types/settings.ts（Settings / ThemeMode / SettingsAPI 接口）
- 新建 src/types/index.ts 统一导出类型
- global.d.ts 导入 settingsAPI 类型声明
- 新建 src/electron/main/settings.ts（读写 exe 同级 data/settings.json）
- main/index.ts 加 settings:load / settings:save IPC 处理器
- preload.ts 暴露 settingsAPI
- 新建 src/store/modules/settings.ts（主题切换 + 持久化）
- App.vue 启动时加载设置

### 图标
- 桌面 logo.png → ICO 转换
- electron-builder.yaml 配置 icon / installerIcon / uninstallerIcon
- 应用窗口图标配置（public/img/icon.ico）

### 文件菜单 + 退出功能
- TitleBar 文件菜单加"退出"项
- preload / main / types 新增 app-quit IPC

### README
- 重写 README.md，覆盖项目完整功能清单、最新目录结构、精确的数据目录行为
- 修复之前草案中的多处不准确描述：数据目录路径、ICO 路径、Sidebar 状态等
